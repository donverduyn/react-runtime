describe('withUpstream', () => {
  it('should work with upstream contexts', () => {
    // Test the integration with upstream contexts
    // This is a placeholder test, you should replace it with actual tests
    expect(true).toBeTruthy();
  });

  it('should work with upstream tags', () => {
    // Test the integration with upstream tags
    // This is a placeholder test, you should replace it with actual tests
    expect(true).toBeTruthy();
  });
});
