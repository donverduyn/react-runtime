describe('withRuntime', () => {
  it('should work with runtime context', () => {
    // Test the integration with runtime context
    // This is a placeholder test, you should replace it with actual tests
    expect(true).toBeTruthy();
  });

  it('should work with runtime tags', () => {
    // Test the integration with runtime tags
    // This is a placeholder test, you should replace it with actual tests
    expect(true).toBeTruthy();
  });
});
